\documentclass[a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage[backend=bibtex]{biblatex}
\bibliography{biblio}

\title{Lattice Signature with Efficient Protocols}
\author{Bataille Alex, Clerbaux Coline\\Fallik Gabriel, Grasso Francesca, Tajani Mohamed}

\begin{document}
\maketitle
\section{Introduction}
Digital signatures are fundamental cryptographic primitives, allowing a user to authenticate a message and guarantee its integrity. For decades,
most practical signature schemes have relied on classical hardness assumptions such as integer factorization or discrete logarithms. However,
these assumptions are known to be broken by quantum algorithms, which motivates the search for alternative constructions secure in the post-quantum setting.
Lattice-based cryptography has emerged as one of the most promising frameworks for post-quantum security, as its underlying problems are believed to remain
hard even for quantum computers. Beyond security, modern applications often require signatures to interact efficiently with zero-knowledge proof systems,
for instance in privacy-preserving protocols. Designing lattice-based signatures that remain compatible with such proof systems is therefore crucial.
A first attempt in this direction was the construction of Libert, Ling, Mouhartem et al. (\cite{LLM+16}), which proposes a lattice-based signature scheme with
efficient protocols. Although conceptually appealing, their construction suffers from extremely large proof sizes (several hundreds of megabytes) making it impractical.
The paper studied in this report (\cite{mainPaper}) revisits this problem and proposes a new lattice-based signature scheme specifically designed to interface smoothly
with recent zero-knowledge argument systems, while reducing the size of the produced proofs. In the following, we analyse the main ideas behind
this construction, highlight its improvements over previous work, and examine some of its technical components in more detail.

\section{Paper Contributions}

\section{Signature Scheme}
The signature scheme proposed by the paper is based on the one studied in \cite{LLM+16}, along with the improvements discussed in the previous section.

\section{Technical Analysis}

\subsection{The \texttt{SampleD} Algorithm}
The main advantage of this algorithm is its efficiency. Hence, we can easily try to make it work for larger dimensions.
The contributors published its implementation on \url{github.com/truncatedsampler/truncated-sampler}.
Unlike previous instantiations that relied heavily on spherical Gaussians, the newer approaches utilize elliptical Gaussian distributions to further compress the signature size and speed up the generation.
This optimization is crucial for reducing the required bounds in the underlying Short Integer Solution (SIS) problem and improving the overall efficiency of the pre-image sampling.

\subsection{The Signature Scheme}
At the core of the proposed construction is a radical redesign of how the signature interacts with the message commitment. In previous state-of-the-art models like \cite{LLM+16}, the commitment to a hidden message required a binary decomposition, which severely deteriorated the quality of the extracted SIS solution and drastically inflated the Zero-Knowledge Proof (ZKP) size.

To solve this issue, the authors propose a much more compact verification equation:
\begin{equation}
    [A | \tau G - B]v = u + Dm \pmod q
\end{equation}
where $A$ and $B$ are public matrices, $G$ is the gadget matrix, $\tau$ is a unique tag, $u$ is a random syndrome, $D$ is the commitment key, and $v$ is the short signature vector.

To sign a hidden message $m$, the user first commits to it via $c = Ar + Dm$, where $r$ is a Gaussian randomness. The central innovation of this scheme is the absorption of this randomness $r$ directly into the pre-image $v'$ generated by the \texttt{SampleD} algorithm. By setting the final signature vector as $v = v' - [r^T | 0]^T$, the protocol removes the need to separately prove the knowledge of the commitment opening. This algebraic simplification has a cascading effect, removing thousands of intermediate witnesses in the subsequent zero-knowledge proof.

Furthermore, the protocol utilizes the tag $\tau$ to prevent replay attacks and ensure uniqueness. In privacy-preserving applications (such as anonymous credentials or e-voting systems), this stateful tag acts as a serial number that guarantees a user cannot double-spend or double-vote, while keeping their actual message $m$ totally secret.

Finally, while the standard lattice implementation reduces the proof size to approximately 18 MB, the authors push the efficiency further by instantiating the scheme over structured lattices,
specifically module-SIS and module-LWE. Coupled with recent exact ZK proof frameworks (such as the Lyubashevsky-Nguyen-Plançon \cite{LNP22} framework),
this approach reduces the size of a proof of possession of a message-signature pair to less than 650 KB. This monumental decrease makes the signature scheme highly practical for real-world,
privacy-enhancing applications.

\subsection{Application to E-Voting Systems}
The technical properties of the proposed signature scheme make it particularly advantageous for electronic voting protocols.
In a secure e-voting scenario, the fundamental requirements are voter privacy (anonymity),
vote unforgeability, and public verifiability of the tally.
The stateful tag $\tau$ integrated into the verification equation provides a mathematically sound mechanism to prevent double-voting.
Specifically, a unique tag can be deterministically derived for each voter per election. When a vote is cast, the tag is revealed, allowing voting authorities to verify that no voter has submitted multiple ballots,
all without compromising the secrecy of the committed vote $m$.

Moreover, the drastic reduction in the size of the zero-knowledge proofs ensures that the protocol remains highly scalable.
In a large-scale election, storing and verifying millions of proofs becomes completely feasible, addressing one of the main bottlenecks of previous lattice-based privacy-preserving systems.
The combination of post-quantum security guarantees, efficient pre-image sampling, and compact proof sizes establishes this scheme as a robust foundation for next-generation e-voting infrastructures.

\section{Conclusion}

\newpage
\printbibliography

\end{document}
